// import path from 'path';
// import { pathToFileURL } from 'url';

// export const readNativeflowConfig = async () => {
//     const fileUrl = pathToFileURL(path.resolve(process.cwd(), 'nativeflow.config.js')).href;
//     const configModule = await import(fileUrl);
//     return configModule.default;
// }
