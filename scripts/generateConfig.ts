// // scripts/generateConfig.ts
// import fs from 'fs-extra';
// import { readNativeflowConfig } from './readConfig';

// async function generate() {
//   const config = await readNativeflowConfig();
//   await fs.writeJSON('src/generated/config.json', config, { spaces: 2 });
// }

// generate();
