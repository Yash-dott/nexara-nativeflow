type sizes = {
    xs: number,
    sm: number,
    md: number,
    lg: number,
    full: number,
};

const dialogSizes: sizes = {
    xs: 60,
    sm: 70,
    md: 80,
    lg: 90,
    full: 100
};

export default dialogSizes;